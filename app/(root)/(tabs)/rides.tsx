import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text className="text-9xl text-black">Rides</Text>
    </SafeAreaView>
  );
};

export default Rides;
